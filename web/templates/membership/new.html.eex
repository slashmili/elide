<h2>New membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_membership_path(@conn, :create, @org.id) %>

<%= link "Back", to: organization_membership_path(@conn, :index, @org.id) %>
