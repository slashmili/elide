<h2>New membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_membership_path(@conn, :create, @organization_id) %>

<%= link "Back", to: organization_membership_path(@conn, :index, @organization_id) %>
