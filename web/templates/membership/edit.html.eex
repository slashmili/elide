<h2>Edit membership</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_membership_path(@conn, 1, :update, @membership) %>

<%= link "Back", to: organization_membership_path(@conn, 1, :index) %>
